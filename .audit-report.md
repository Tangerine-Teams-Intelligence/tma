# Pre-Public Audit — `tangerine-meeting-live`

**Date:** 2026-04-25
**Auditor:** Claude Opus 4.7 (1M context)
**Scope:** All 18 commits across all branches
**Purpose:** Determine whether the repo is safe to make public (or transfer + rename to `tma` under `Tangerine-Teams-Intelligence`).

---

## 1. Secrets Found

**Result: NONE.**

Patterns searched across full history (`git log --all -p`):
- `sk-[a-zA-Z0-9]{20,}` (OpenAI keys)
- `sk-proj-`, `sk_(live|test)_*` (OpenAI/Stripe variants)
- `xoxb-` (Slack bot tokens)
- `ghp_*` (GitHub personal access tokens)
- `AKIA[0-9A-Z]{16}` (AWS access key IDs)
- `aws_secret`
- `BEGIN (RSA|OPENSSH|EC|DSA|PGP) PRIVATE KEY`
- `client_secret`, `bearer <token>`
- `MTk*` / `MT[ABCNI]*` (Discord bot token format)
- `nvapi-*`, `hf_*` (NVIDIA / HuggingFace)
- `OPENAI_API_KEY=<value>`, `DISCORD_BOT_TOKEN=<value>` with content

**Hits: 0 with actual credential values.**

Only matches were:
- Function names (`safeInvoke`, `safer-buffer`, `unsafe-libyaml`)
- Variable names in code (`token: string`, `client_id` field references — no values)
- Comments and prose mentioning "tokens" / "safe" / "client_id" generically

The lone `.env.example` (commit `8e033ff`, file `bot/.env.example`) is a template with `DISCORD_BOT_TOKEN=` and `OPENAI_API_KEY=` set to empty strings — no secrets leaked.

---

## 2. .env Files in History

| Commit | Path | Notes |
|---|---|---|
| `8e033ff` | `bot/.env.example` | Template only; values empty. SAFE. |

No `.env` (without `.example`) has ever been committed. `.gitignore` includes `.env*` since first commit.

---

## 3. Credential Files

Searched for: `secrets.json`, `credentials.json`, `*.pem`, `*.key`, `*.p12`, `*.pfx` in history.

**Result: NONE found.**

---

## 4. Sensitive / Proprietary Content

**Result: SUBSTANTIAL — needs scrub before public release.**

Two commits introduced internal Tangerine IP and personal/financial info that should not be public:

### 4a. Commit `423cde1b` — "Initial commit: dual-stream meeting transcription + Claude observer"

File: `CLAUDE.md` (later moved to `legacy/CLAUDE.md`)

Sensitive lines (current path `legacy/CLAUDE.md`):
- L110: `20 active patents (TI-2026-001 through 021, 015 archived into 010).`
- L112: `F-1 visa: no salary, equity only.`
- L113: `Investor funds never flow directly to HK or WFOE.`
- L116: `US: Tangerine Intelligence Inc. (Delaware C-Corp, File #10552429)`
- L117: `HK pending, Shenzhen Qianhai WFOE pending`
- L119: `SAFE: father HK $80K + mother $300K, both at $5M cap`

Plus: equity split (40/30/3.5/26.5), product spec (iFactory: watch + gateway + dock, no badge), legal red lines (PIPL handling, "efficiency analysis tool" positioning).

### 4b. Commit `8e033ff7` — "TMA v0.1.0 OSS-ready release"

Files in `demo/2026-04-22-david-tma-kickoff/` and `demo/target-knowledge-repo-snapshot/`:

| File | Sensitive content |
|---|---|
| `demo/2026-04-22-david-tma-kickoff/transcript.md` (L16) | `4/30 截止 6 天。P0 八件 docx 我 4/24 跑了一遍 audit——TI-2026-006 还有 1 FAIL，Claim 1 ARM detail.` |
| `demo/2026-04-22-david-tma-kickoff/summary.md` (L26, L43) | `advisor 4/26 给 RFP draft + TI-2026-006 ARM detail 修复;` `TI-2026-006 Claim 1 ARM detail 修复（by 2026-04-25）` |
| `demo/2026-04-22-david-tma-kickoff/knowledge-diff.md` (L57) | `TI-2026-006 Claim 1 ARM detail 修复：deadline 2026-04-25（advisor）` |
| `demo/2026-04-22-david-tma-kickoff/intents/advisor.md` (L13) | `同步 4/30 律师 RFP 截止前 P0 八件 docx 的最终状态——TI-2026-006 还有 1 FAIL` |
| `demo/target-knowledge-repo-snapshot/knowledge/session-state.md` (L13) | Same TI-2026-006 deadline |

These files reveal:
- Specific patent ID under review (TI-2026-006) and which claim has open issues (Claim 1 ARM detail)
- Patent attorney RFP timeline (4/30 deadline)
- Internal P0 patent batch labeling

This is exactly the kind of "internal IP that shouldn't be public" called out in the audit prompt. Patent prosecution status disclosure could weaken claim novelty arguments and signal areas of weakness to potential infringers.

---

## 5. Verdict

**NEEDS SCRUB — not safe to publish as-is.**

No live credentials are exposed (the technical secret-scan is clean), but two commits embed internal patent prosecution detail, family financing structure, F-1 / equity split, and HK/WFOE corporate plans. None of this should be publicly readable on GitHub.

### Recommended scrub plan (CEO's call — not executed)

Two viable paths:

**Option A — surgical history rewrite (preserves commit graph, minimal):**
1. `git filter-repo --path legacy/CLAUDE.md --invert-paths` to drop the legacy file entirely.
2. `git filter-repo --path demo/2026-04-22-david-tma-kickoff --invert-paths` and `--path demo/target-knowledge-repo-snapshot --invert-paths` to drop the kickoff demo + snapshot.
3. Replace with a generic synthetic demo before re-publishing (so SETUP.md still works).
4. Force-push to a fresh repo (or after transfer, force-push the cleaned history).

**Option B — squash-and-republish (cleanest, breaks history):**
1. New orphan branch with only the current working tree minus the two demo dirs and `legacy/CLAUDE.md`.
2. Single squashed initial commit ("Initial public release v0.1.0").
3. Push as new repo, archive old private one.

Either option also requires:
- Sanitize `legacy/CLAUDE.md` or remove the `legacy/` dir entirely (it's labelled legacy already).
- Rebuild `demo/` from a synthetic non-Tangerine meeting OR a Tangerine meeting whose content is intentionally public-safe (no patent IDs, no financing, no advisor names tied to internal deadlines).
- Re-audit before publish.

**Until scrub is done: keep repo private. Do not flip visibility, do not transfer-without-rewrite to the new org.**
